package com.example.controlefluxo.view.listener

interface OnPessoaListener {
    fun onClick(id: Int)                      //responder por um click
    fun OnDelete(id: Int)                     //responder por um delete
}